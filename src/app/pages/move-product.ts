import { Component, Input } from '@angular/core';

@Component({
  selector: 'user-info',
  template: `
    <div class="container-fluid">
      <dl class="row">
      </dl>
    </div>
  `
})
export class MoveComponent {
}
